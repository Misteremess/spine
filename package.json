{
  "name": "spine",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev:api": "pnpm --filter @spine/api dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
