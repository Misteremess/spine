packages:
  - "apps/*"
  - "packages/*"
# Metro (React Native) no soporta el layout aislado con symlinks de pnpm
nodeLinker: hoisted
allowBuilds:
  esbuild: true
  # Optimizador de imágenes de Next; la web usa <img> directo, no necesita el binario.
  sharp: false
